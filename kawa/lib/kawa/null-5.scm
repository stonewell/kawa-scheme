(define-library (kawa null-5)
  (import (scheme r5rs))
  (export
   and
   begin
   case
   cond
   define
   delay
   do
   define-syntax
   if
   let
   let*
   letrec
   let-syntax
   letrec-syntax
   lambda
   or
   quasiquote
   quote
   set!
   syntax-rules))
